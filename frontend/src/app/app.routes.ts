import { Routes } from '@angular/router';
import { SignupFormComponent } from '../signup-form/signup-form.component';

export const routes: Routes = [
    { path: '', component: SignupFormComponent, title: 'Signup Form'}
];
